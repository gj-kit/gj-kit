{
  "name": "gj-kit",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "test:types": "corepack pnpm -r test:types",
    "check:readme": "corepack pnpm --filter @gj-kit/expo-ui check:readme && corepack pnpm --filter @gj-kit/expo-media check:readme && corepack pnpm --filter @gj-kit/expo-workouts check:readme && corepack pnpm --filter @gj-kit/toss-payments check:readme && corepack pnpm --filter @gj-kit/toss-payments-nestjs check:readme && corepack pnpm --filter @gj-kit/toss-payments-postgresql check:readme",
    "check:pack": "node scripts/check-pack-contents.mjs",
    "check:toss-payments-consumer": "node scripts/check-toss-payments-consumer.mjs",
    "check:expo-sdk": "corepack pnpm --filter @gj-kit/expo-media check:expo-sdk",
    "check:expo-ui-consumer": "node scripts/check-expo-ui-consumer.mjs",
    "check:expo-media-consumer": "node scripts/check-expo-media-consumer.mjs",
    "check:expo-workouts-consumer": "node scripts/check-expo-workouts-consumer.mjs",
    "check:docs": "corepack pnpm --filter @gj-kit/expo-ui-docs export:web && corepack pnpm --filter @gj-kit/expo-ui-docs check:seo",
    "verify:release": "corepack pnpm run build && corepack pnpm run typecheck && corepack pnpm run test && corepack pnpm run test:types && corepack pnpm run check:readme && corepack pnpm run check:pack && corepack pnpm run check:toss-payments-consumer && corepack pnpm run check:expo-sdk && corepack pnpm run check:expo-ui-consumer && corepack pnpm run check:expo-media-consumer && corepack pnpm run check:expo-workouts-consumer && corepack pnpm run check:docs",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "publish:npm": "changeset publish && node scripts/publish-github-packages.mjs",
    "release:npm": "corepack pnpm run verify:release && corepack pnpm run publish:npm"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
