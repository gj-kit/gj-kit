import type { UseEscapeKeyOptions } from './use-escape-key-types';

/** Native Modal owns hardware-back delivery; there is no document Escape source. */
export function useEscapeKey(_options: UseEscapeKeyOptions): void {}

export type { UseEscapeKeyOptions } from './use-escape-key-types';
