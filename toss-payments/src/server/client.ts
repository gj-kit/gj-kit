// Phase 3에서 구현 — createTossClient (Basic 인증 base64(secretKey + ":") 내부 캡슐화, 공개 API 없음).
// ApiSecretKey·WidgetSecretKey 이중 수용(KeyKind 각인), getPayment/getPaymentByOrderId, cancels 네임스페이스.
export type TODO_ServerClient = never;
