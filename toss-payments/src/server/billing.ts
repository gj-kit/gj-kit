// Phase 3에서 구현 — parseBillingAuthCallback / confirmPendingAuth / createBillingFlow
// (AuthKeyReceived → issue → BillingProfile(봉인) → approve, BillingKeyStore 필수 주입).
export type TODO_ServerBilling = never;
