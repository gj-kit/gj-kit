{
  // 테스트 전용 — 웹 경로 테스트가 DOM 타입을 요구한다 (expo-ui 선례).
  // src의 무DOM 규율은 tsconfig.core.json이 따로 검사하므로 여기서 DOM을 켜도
  // 라이브러리 코드의 DOM 유입은 여전히 차단된다.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src", "tests"]
}
