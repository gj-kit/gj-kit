// Phase 3에서 구현 — parseSuccessCallback / parseFailCallback / createConfirmFlow
// (createOrder → verify → confirm 3단계 + confirmCallback 원스톱, OrderStore 필수 주입).
export type TODO_ServerConfirm = never;
