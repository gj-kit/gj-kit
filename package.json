{
  "name": "gj-kit",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:types": "pnpm -r test:types",
    "changeset": "changeset",
    "version:packages": "changeset version",
    "release:npm": "pnpm run build && changeset publish && node scripts/publish-github-packages.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
